agent:
  plugin:
    #只允许以下插件有效，如果不指定，则全部有效
    enabledPlugins:
      - com.org.test.def.UserAndRoleMemberMethodDef
    #只允许以下插件定义有效， 如果不指定，则全部有效
    enabledPluginGroups:
      - com.org.test.SimplePluginGroup




